# Favicon Images

Place your favicon images (e.g. `favicon.ico`, `icon.png`, `apple-icon.png`, `favicon-32x32.png`, etc.) in this folder.

In a Next.js application, these static assets are served from the `/favicon/` path.
